In the end of Part VI, we'll have finished the Home screen, with a drawer that only loads the matches when pulled and some card buttons that have a ripple effect when pressed.

The episode has already been uploaded in YT:
* [Part VI - Finished Home Screen](https://youtu.be/nKT-IrxS3uU?feature=shared&t=11)

Sections in the video:
* Review what was done so far
* Increase header font size using the correct CSS variable
* Increasing the font size in the drawer and aligning right the # of matches
* Adding the match type to the match list with color depending on the type of match
* Adding a search box and filtering in the aggregate for matches 
* Fixing bug that was causing the drawer to close on click
* Adding dynamic opacity to the drawer content while dragging
* Animating the border radius to change while the drawer is being dragged
* Load the number of matches in the list and use animations to blur it while waiting for server response
* Get match list only on demand to improve performance
* Adding two cards with image background for main options in the screen
* Transform cards into a reusable block with Ripple effect on card click
* Showing how to use Paint Rendering feature of Chrome Dev Tools and fix an issue with animation
* Fixing an issue cause by using client actions and JavaScript to manipulate DOM, using $resolve();

<img src="https://github.com/bazinga-thedog/from-an-idea-to-an-outsystems-app/assets/172836307/0845a939-790c-47d6-ac80-106bc8876078" width="300">
