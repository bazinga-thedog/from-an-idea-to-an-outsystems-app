Here is the Python script to run the importer

In this folder, create a virtual environment, run in the command line:

```powershell
python -m venv ./ 

.\Scripts\activate

pip install requests

python .\importer.py
```