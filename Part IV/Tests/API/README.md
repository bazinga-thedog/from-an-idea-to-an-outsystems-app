These are Python scripts to run the API tests using pytest

In this folder, create a virtual environment, run in the command line:

```powershell
python -m venv ./ 

.\Scripts\activate

pip install pytest

pip install requests

python -m pytest -v .\API\
```

![image](https://github.com/bazinga-thedog/from-an-idea-to-an-outsystems-app/assets/172836307/2cccec4e-781c-4e2e-b36b-32a513174f04)
